dockerbuild:

  build_args:
    cuda_version:
      key: cuda
      name: cuda
      versions:
        - "10.1"
        - "11.1"

    ubuntu_version:
      key: ubuntu
      versions:
        - "20.04"
