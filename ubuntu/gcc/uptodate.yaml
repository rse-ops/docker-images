dockerbuild:
  build_args:
    gcc_version:
      key: gcc
      versions:
       - "9.4.0"
       - "10.3.0"
       - "11.2.0"

    # Look for ubuntu versions for our base builds
    ubuntu_version:
      key: ubuntu
      versions:
        - "20.04"
