dockerbuild:

  build_args:
    llvm_amdgpu_version:
      key: llvm-amdgpu
      versions:
        - "5.3.0"
        - "5.2.3"

    # Look for ubuntu versions for our base builds
    ubuntu_version:
      key: ubuntu
      versions:
        - "20.04"
